spring:
  application:
    name: app
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true
      path: /h2-console
  sql:
    init:
      mode: always
app:
  jwtSecret: Qazxsw2134e._
  jwtExpirationMls: 36000000
  publicUrls:
    - "/swagger-ui.html**"
    - "/v3/**"
    - "/swagger-ui/**"
    - "/api/v1/auth/**"

